#include "CerberusTools/CT_EditorWorld.h"

void EditorWorld::BoxOperation(Vector3 Bound1, Vector3 Bound2, bool Additive)
{

	int x1 = Bound1.x - Bound2.x;
	int y1 = Bound1.y - Bound2.y;


	for (int x = 0; x < x1; x++)
	{
		for (int y = 0; y < y1; y++)
		{
			
		}
	}

	




}
